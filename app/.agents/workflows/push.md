---
description: Hace commit y push automático de todos los cambios
---
// turbo-all

1. Ejecuta `git add .` en la terminal.
2. Ejecuta `git commit -m "update: cambios sugeridos y auto-commit"` en la terminal.
3. Ejecuta `git push origin main` en la terminal.
